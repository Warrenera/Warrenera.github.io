[project]
name = "cAnnections Test Framework"
version = "0.0.1"

[tool.pytest]
addopts = ["--import-mode=importlib"]
strict = true

[tool.ruff]
line-length = 99

[tool.ruff.lint]
select = ["ALL"]
ignore = ["CPY001"]
